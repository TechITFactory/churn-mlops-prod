line-length = 100
target-version = "py310"

[lint]
# Keep course-friendly, high-value rules
select = ["E", "F", "I", "B"]
ignore = ["E501"]

[lint.per-file-ignores]
"tests/*" = ["B011"]

[lint.isort]
known-first-party = ["churn_mlops"]
